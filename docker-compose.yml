services:
  videoforge:
    build: .
    ports:
      - "${PORT:-5050}:${PORT:-5050}"
    env_file:
      - .env
    volumes:
      - ./storage:/app/storage
      - ./db:/app/db
    restart: unless-stopped
    environment:
      - NODE_ENV=production
